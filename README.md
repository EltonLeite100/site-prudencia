# site-prudencia

Código-fonte do site Prudência (React + Vite).

## Publicação

- `main`: código-fonte
- `gh-pages`: publicação automática via GitHub Pages (workflow em `.github/workflows/deploy-pages.yml`)

## Comandos

- `npm install`
- `npm run dev`
- `npm run build`
